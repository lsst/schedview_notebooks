title: prenight
description: Pre-night briefing
authors:
  - name: Eric H. Neilsen, Jr.
    slack: neilsen
parameters:
  day_obs:
    type: string
    description: Date of observation (YYYY-MM-DD)
    default: "2025-05-02"
  sim_date:
    type: string
    description: Date of the simulation (YYYY-MM-DD)
    default: "2024-05-07"
  sim_index:
    type: string
    description: The index of the simulation (within its date)
    default: "1"
